@* @model ModelDimJson
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Search Transcript by Telephone</h2>

<form asp-action="ShowTranscript" method="post">
    <div class="form-group">
        <label for="telephoneNo">Telephone Number</label>
        <input type="text" class="form-control" id="telephoneNo" name="telephoneNo" required />
    </div>
    <button type="submit" class="btn btn-primary">Search</button>
</form>

@if (!string.IsNullOrEmpty(ViewBag.Error))
{
        <div class="alert alert-danger mt-3">@ViewBag.Error</div>
}

@if (Model != null && Model.Sentences != null && Model.Sentences.Any())
{
        <h4 class="mt-4">Transcript Sentences</h4>
        <ul class="list-group mt-2">
        @foreach (var sentence in Model.Sentences)
        {
                    <li class="list-group-item">@sentence.Text</li>
        }
        </ul>
}
 *@